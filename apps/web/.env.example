# Website-origin proxy. Provider keys and RPC URLs belong ONLY in apps/backend/.env.
POWERCHAIN_API_URL=http://localhost:4000
POWERCHAIN_INTERNAL_API_TOKEN=

# Canonical public website origin used only for sitemap/metadata.
NEXT_PUBLIC_SITE_URL=https://powerchain.org
