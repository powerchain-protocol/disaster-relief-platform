import { SiteHeader, SiteFooter } from "../../components/site-shell";
import { ProviderStatusStrip } from "../../components/provider-status-strip";
export default function Page(){return <main id="main"><SiteHeader/><section className="shell page-hero"><span className="eyebrow">SOURCE-AWARE NETWORK</span><h1>Settlement truth, provider resilience and explicit evidence boundaries.</h1><p>Solana supplies settlement truth; Postgres supplies operational truth; evidence remains off-chain where size, privacy or workflow require it. Provider fallback is bounded and cannot change authoritative on-chain fields.</p></section><section className="shell network-status-wrap"><ProviderStatusStrip/></section><section className="section section-muted"><div className="shell content-card"><span className="eyebrow">OPERATING MODEL</span><h2>Sense → Verify → Fund → Route → Protect → Respond → Prove → Audit</h2><p>RPC failover is limited to configured endpoints. Market-provider fallback is limited to analytics and display. Required program IDs, executable fingerprints, balances, settlement state and authoritative token supply never fall back to guessed values.</p></div></section><SiteFooter/></main>}
